import React from "react";
import "./Panel.css"
// The ...props means, spread all of the passed props onto this element
// That way we don't have to define them all individually

const Panel = props => (
  <div>
    <h1> Overwatch Panel </h1>
  </div>
);

export default Panel;
